<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authFragment">
    <fragment
        android:id="@+id/authFragment"
        android:name="com.mobility.inclass07.AuthDirectory.AuthFragment"
        android:label="fragment_auth"
        tools:layout="@layout/fragment_auth">
        <action
            android:id="@+id/action_authFragment_to_surveyForm"
            app:destination="@id/surveyForm"
            app:popUpToInclusive="true"
            app:popUpTo="@id/authFragment"/>
    </fragment>
    <fragment
        android:id="@+id/surveyForm"
        android:name="com.mobility.inclass07.Judges.SurveyForm"
        android:label="fragment_survey_form"
        tools:layout="@layout/fragment_survey_form" />
</navigation>